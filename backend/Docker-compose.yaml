services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MONGO_URI=mongodb://mongo:27017/urlshortener
      - REDIS_HOST=redis
      - REDIS_PORT=6379
    env_file:
      - .env

